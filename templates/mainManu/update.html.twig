{% extends 'base2.html.twig' %}

{% block body %}
	<h1>Update Data</h1>
	{# {{  form('insert_form')  }} #}
	{{ form_start(insert_form) }}
	{# {{ form_row(insert_form.name) }} #}
	{{ form_label(insert_form.name) }}
	{{ form_widget(insert_form.name) }}
	{{ form_label(insert_form.address) }}
	{{ form_widget(insert_form.address,) }}
	{{ form_label(insert_form.number) }}
	{{ form_widget(insert_form.number,) }}
	{{ form_label(insert_form.compony) }}
	{{ form_widget(insert_form.compony,) }}
	{{ form_widget(insert_form.Submit,{'label':"Update Data"}) }}
	{# {{ form_widget(insert_form.name,{value:emp.name}) }}
	{{ form_widget(insert_form.address,{value:emp.address}) }}
	{{ form_widget(insert_form.number,{value:emp.number}) }}
	{{ form_widget(insert_form.compony,{value:emp.compony}) }} #}
	{# {{ form_row(insert_form.address) }} #}
	{{ form_end(insert_form) }}

    {% endblock %}
